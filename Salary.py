# This program inputs the hours worked and rate per hour and calculate the pay of the worker 

hrs = input("Enter Hours:")
rate= input("Enter Rate:")
grossPay=(float(hrs)*float(rate))
print("Pay:",grossPay)
